---
- name: Create users only in some nodes
  hosts: all
  become: true
  gather_facts: false
  tasks:
      - name: create user
        user:
             name: '{{ item }}'
             state: 'absent'
             remove: true
        loop:
           - user1
           - user2
           - user3
        when: ('node1' in group_names)
